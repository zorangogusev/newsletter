const  initState = {
    isLoggedIn: false
}

const UserAuthReducer = (state = initState, action) =>
{
    switch(action.type)
    {

        default:
            return state
    }
}

export default UserAuthReducer;
